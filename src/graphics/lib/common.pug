meta(charset="UTF-8")
script(src=`https://use.typekit.net/${process.env.TYPEKIT_ID}.js`)
script.
	try { Typekit.load({ async: true }); } catch (e) { }
link(rel="stylesheet", href=`${__dirname}/../lib/common.css`)
link(rel="stylesheet", href=`${__dirname}/../fonts/migmix-2p.css`)
